# AI Data Scientist Agent 

## Overview
Streamlit app with electric-blue neon theme, avatar (Devika), auto-detect + dropdown target selection, AutoML, and Plotly visualizations.

## Run
1. Create virtual environment and activate:
```bash
python -m venv venv
venv\Scripts\activate      # Windows
# or: source venv/bin/activate  # macOS / Linux
